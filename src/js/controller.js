import * as model from "./model.js";

// const controlTry = async function () {
//   const id = 1;
//   const chelsea = await model.getPlayers();
//   console.log(chelsea.data.find((team) => team.id === id));
// };

// controlTry();
